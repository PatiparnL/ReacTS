import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => ({
    iconHeader: {
        position: 'relative',
        top: '4px',
        marginRight: '0.5em'
    },
    buttoncompare:{
        float: 'right',
        marginRight: '1em',
    }
}));